# Student-Result Application Service
apiVersion: v1
kind: Service
metadata:
  name: student-result-service
  namespace: student-result
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 5050
  selector:
    app: student-app
